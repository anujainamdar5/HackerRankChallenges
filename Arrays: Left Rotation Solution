public class Solution {

    static int[] rotLeft(int[] a, int d) {
        int[] rotatedA = new int[a.length];
        d=d%a.length;
        for(int i=0;i<a.length;i++)
            rotatedA[i]=a[(i+d)%a.length];
        return rotatedA;
    }
}
